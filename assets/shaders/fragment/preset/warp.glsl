precision highp float; precision highp int; uniform sampler2D tDiffuse; uniform vec2 uvScale; uniform vec2 resolution; varying vec2 vUv; uniform float Warp_Style; uniform float Warp_Axis; uniform float Bend; uniform float Horizontal_Distortion; uniform float Vertical_Distortion; const float PI = 3.14159265358979323846; float sat(float x) { return clamp(x, 0.0, 1.0); } vec2 applyAxis(vec2 p, int axis) { return (axis == 1) ? p.yx : p; } vec2 unapplyAxis(vec2 p, int axis) { return (axis == 1) ? p.yx : p; } vec2 distortionForward(vec2 p, float hd, float vd) { float x = p.x; float y = p.y; float hdPos = max(hd, 0.0); float hdNeg = max(-hd, 0.0); float yScaleLeft = (1.0 + x) * 0.5; float yScaleRight = (1.0 - x) * 0.5; float yScale = 1.0; yScale = mix(yScale, max(yScaleLeft, 1e-4), hdPos); yScale = mix(yScale, max(yScaleRight, 1e-4), hdNeg); y *= yScale; float vdPos = max(vd, 0.0); float vdNeg = max(-vd, 0.0); float xScaleTop = (1.0 - y) * 0.5; float xScaleBottom = (1.0 + y) * 0.5; float xScale = 1.0; xScale = mix(xScale, max(xScaleTop, 1e-4), vdPos); xScale = mix(xScale, max(xScaleBottom, 1e-4), vdNeg); x *= xScale; return vec2(x, y); } vec2 warpRadialForward(vec2 p, int style, float b, float aspect) { vec2 q = vec2(p.x * aspect, p.y); float r = length(q); float ang = atan(q.y, q.x); float k = 1.0 + abs(b) * 1.25; float expn; if (style == 11) { expn = (b >= 0.0) ? (1.0 / max(k, 1e-4)) : max(k, 1e-4); } else { expn = (b >= 0.0) ? max(k, 1e-4) : (1.0 / max(k, 1e-4)); } float rr = pow(max(r, 1e-6), expn); vec2 qWarp = vec2(cos(ang), sin(ang)) * rr; float w = 1.0; if (style == 11) { float rEdge = max(abs(p.x), abs(p.y)); w = pow(clamp(1.0 - rEdge, 0.0, 1.0), 2.8); } q = mix(q, qWarp, w); q.x /= aspect; return q; } float bendEffective(int style, int axis, float b) { if (axis != 1) return b; if (style == 0 || style == 1 || style == 2 || style == 3) return -b; return b; } vec2 warpStyleForward(vec2 p, int style, int axis, float b) { float u = p.x; float v = p.y; float uu = u*u; float vv = v*v; float arcU = 1.0 - uu; float midV = 1.0 - vv; float radial = sat(1.0 - (uu + vv)); if (style == 0) { float a = abs(b); float thetaMax = max(a * (PI * 0.60), 1e-5); float theta = u * thetaMax; float edge = (1.0 - cos(theta)); float edgeMax = max(1.0 - cos(thetaMax), 1e-5); float bulge = 1.0 - (edge / edgeMax); float t = (v + 1.0) * 0.5; float bottom = 1.0 - t; float wV = mix(0.30, 1.0, bottom); v += b * 0.95 * bulge * wV; float flare = 1.0 + a * 0.55 * bulge * bottom; u *= flare; return vec2(u, v); } if (style == 1) { float bottomMask = (1.0 - v) * 0.5; float m = bottomMask; v += b * 0.92 * arcU * m; return vec2(u, v); } if (style == 2) { float topMask = (1.0 + v) * 0.5; float m = topMask; v += b * 0.92 * arcU * m; return vec2(u, v); } if (style == 3) { float a = abs(b); float thetaMax = max(a * (PI * 0.70), 1e-5); float theta = u * thetaMax; float edge = (1.0 - cos(theta)); float edgeMax = max(1.0 - cos(thetaMax), 1e-5); float bulge = 1.0 - (edge / edgeMax); float h = 0.85 + 0.15 * (1.0 - abs(v)); v += b * 1.10 * bulge * h; return vec2(u, v); } if (style == 4) { float k = b * 0.60 * radial; u *= 1.0 + k; v *= 1.0 + k; return vec2(u, v); } if (style == 5) { float a = abs(b); float m = (1.0 - v) * 0.5; m = pow(m, 1.65); float taper = (m * 2.0 - 1.0); float scaleU = 1.0 + a * 0.55 * taper; scaleU = max(scaleU, 0.15); u *= scaleU; float bulge = arcU; float bendAmt = b * 0.95 * bulge * (0.25 + 0.75 * m); v += bendAmt; return vec2(u, v); } if (style == 6) { float a = abs(b); float m = (1.0 + v) * 0.5; m = pow(m, 1.65); float taper = (m * 2.0 - 1.0); float scaleU = 1.0 + a * 0.55 * taper; scaleU = max(scaleU, 0.15); u *= scaleU; float bulge = arcU; float bendAmt = b * 0.95 * bulge * (0.25 + 0.75 * m); v += bendAmt; return vec2(u, v); } if (style == 7) { v += b * 0.55 * sin((u * 0.5 + 0.5) * PI); return vec2(u, v); } if (style == 8) { v += b * 0.40 * sin((u * 0.5 + 0.5) * PI * 2.0); return vec2(u, v); } if (style == 9) { float k = b; float a = abs(b); float uu2 = u * u; float vv2 = v * v; float midU2 = 1.0 - uu2; float midV2 = 1.0 - vv2; float belly = 1.0 + 0.45 * a * midU2 * (0.60 + 0.40 * midV2); u *= belly; float pinch = 1.0 - 0.50 * a * midU2; v *= pinch; float asym = 0.38 * k * midV2 * (-u); float s = 1.0 + asym; s = clamp(s, 0.15, 3.0); u *= s; v += 0.06 * k * (uu2 - 0.5) * midV2; return vec2(u, v); } if (style == 10) { float u01 = u * 0.5 + 0.5; float ramp = u01 * u01 * (3.0 - 2.0 * u01); float ramp2 = ramp * ramp; v += b * (1.05 * ramp + 0.50 * ramp2); return vec2(u, v); } if (style == 13) { float maxTwist = 1.35 * PI; float r = max(abs(u), abs(v)); float fall = 1.0 - r; fall = pow(max(fall, 0.0), 2.2); float ang = -b * maxTwist * fall; float cs = cos(ang); float sn = sin(ang); float u2 = cs * u - sn * v; float v2 = sn * u + cs * v; return vec2(u2, v2); } if (style == 14) { vec2 q = (axis == 1) ? vec2(v, u) : vec2(u, v); float amt = abs(b); float k = 0.92; float midX = 1.0 - (q.x * q.x); float midY = 1.0 - (q.y * q.y); if (b < 0.0) { float sx = 1.0 - amt * k * midY; sx = max(sx, 0.08); q.x *= sx; } else if (b > 0.0) { float sy = 1.0 - amt * k * midX; sy = max(sy, 0.08); q.y *= sy; } vec2 outv = (axis == 1) ? vec2(q.y, q.x) : q; return outv; } return vec2(u, v); } vec2 forwardTransform(vec2 p, int style, int axis, float b, float hd, float vd, float aspect) { if (style == 11 || style == 12) { p = warpRadialForward(p, style, b, aspect); p = distortionForward(p, hd, vd); return p; } float bEff = bendEffective(style, axis, b); vec2 pa = applyAxis(p, axis); pa = warpStyleForward(pa, style, axis, bEff); p = unapplyAxis(pa, axis); p = distortionForward(p, hd, vd); return p; } vec2 inverseSolve(vec2 pOut, int style, int axis, float b, float hd, float vd, float aspect) { vec2 p = pOut; for (int i = 0; i < 7; i++) { vec2 f = forwardTransform(p, style, axis, b, hd, vd, aspect); vec2 e = f - pOut; p -= e * 0.75; } return p; } void main() { vec2 pOut = (vUv - 0.5) * 2.0; float aspect = resolution.x / max(resolution.y, 1.0); int style = int(floor(Warp_Style + 0.5)); int axis = int(floor(Warp_Axis + 0.5)); float b = clamp(Bend / 100.0, -1.0, 1.0); float hd = clamp(Horizontal_Distortion / 100.0, -1.0, 1.0); float vd = clamp(Vertical_Distortion / 100.0, -1.0, 1.0); vec2 pIn = inverseSolve(pOut, style, axis, b, hd, vd, aspect); vec2 uvIn = pIn * 0.5 + 0.5; vec2 sampleUv = uvIn * uvScale; if (uvIn.x < 0.0 || uvIn.x > 1.0 || uvIn.y < 0.0 || uvIn.y > 1.0) { gl_FragColor = vec4(0.0); } else { gl_FragColor = texture2D(tDiffuse, sampleUv); } }
