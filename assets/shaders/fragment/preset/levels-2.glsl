precision highp float; precision highp int; uniform sampler2D tDiffuse; varying vec2 vUvScaled; uniform float Channel; uniform float RGB_Input_Black; uniform float RGB_Input_White; uniform float RGB_Gamma; uniform float RGB_Output_Black; uniform float RGB_Output_White; uniform float Red_Input_Black; uniform float Red_Input_White; uniform float Red_Gamma; uniform float Red_Output_Black; uniform float Red_Output_White; uniform float Green_Input_Black; uniform float Green_Input_White; uniform float Green_Gamma; uniform float Green_Output_Black; uniform float Green_Output_White; uniform float Blue_Input_Black; uniform float Blue_Input_White; uniform float Blue_Gamma; uniform float Blue_Output_Black; uniform float Blue_Output_White; uniform float Alpha_Input_Black; uniform float Alpha_Input_White; uniform float Alpha_Gamma; uniform float Alpha_Output_Black; uniform float Alpha_Output_White; uniform float Clip_To_Output_Black; uniform float Clip_To_Output_White; uniform vec2 uTexelSize; float clamp255(float v) { return clamp(v, 0.0, 255.0); } float gammaSafe(float g) { return clamp(g, 1e-4, 5.0); } float signedPow(float x, float p) { float ax = abs(x); float sx = sign(x); return sx * pow(max(ax, 0.0), p); } float applyLevels1( float x, float inB, float inW, float gammaV, float outB, float outW, bool clipB, bool clipW ){ float iB = clamp255(inB) / 255.0; float iW = clamp255(inW) / 255.0; float oB = clamp255(outB) / 255.0; float oW = clamp255(outW) / 255.0; float denom = iW - iB; if (abs(denom) < 1e-6) denom = (denom < 0.0) ? -1e-6 : 1e-6; float t = (x - iB) / denom; float g = gammaSafe(gammaV); float p = 1.0 / g; t = signedPow(t, p); float y = mix(oB, oW, t); float mn = min(oB, oW); float mx = max(oB, oW); if (clipB) y = max(y, mn); if (clipW) y = min(y, mx); return y; } vec3 blur9(vec2 uv, float radiusPx) { vec2 r = uTexelSize * radiusPx; vec3 c = vec3(0.0); float w = 0.0; c += texture2D(tDiffuse, uv).rgb * 4.0; w += 4.0; c += texture2D(tDiffuse, uv + vec2( r.x, 0.0)).rgb * 2.0; c += texture2D(tDiffuse, uv + vec2(-r.x, 0.0)).rgb * 2.0; c += texture2D(tDiffuse, uv + vec2(0.0, r.y)).rgb * 2.0; c += texture2D(tDiffuse, uv + vec2(0.0, -r.y)).rgb * 2.0; w += 8.0; c += texture2D(tDiffuse, uv + vec2( r.x, r.y)).rgb; c += texture2D(tDiffuse, uv + vec2(-r.x, r.y)).rgb; c += texture2D(tDiffuse, uv + vec2( r.x, -r.y)).rgb; c += texture2D(tDiffuse, uv + vec2(-r.x, -r.y)).rgb; w += 4.0; return c / max(w, 1e-6); } void main() { vec4 texel = texture2D(tDiffuse, vUvScaled); vec3 originalRGB = texel.rgb; bool clipB = (int(floor(Clip_To_Output_Black + 0.5)) == 0); bool clipW = (int(floor(Clip_To_Output_White + 0.5)) == 0); int ch = 4; if (ch == 0) { texel.r = applyLevels1(texel.r, RGB_Input_Black, RGB_Input_White, RGB_Gamma, RGB_Output_Black, RGB_Output_White, clipB, clipW); texel.g = applyLevels1(texel.g, RGB_Input_Black, RGB_Input_White, RGB_Gamma, RGB_Output_Black, RGB_Output_White, clipB, clipW); texel.b = applyLevels1(texel.b, RGB_Input_Black, RGB_Input_White, RGB_Gamma, RGB_Output_Black, RGB_Output_White, clipB, clipW); } else if (ch == 1) { texel.r = applyLevels1(texel.r, RGB_Input_Black, RGB_Input_White, RGB_Gamma, RGB_Output_Black, RGB_Output_White, clipB, clipW); } else if (ch == 2) { texel.g = applyLevels1(texel.g, RGB_Input_Black, RGB_Input_White, RGB_Gamma, RGB_Output_Black, RGB_Output_White, clipB, clipW); } else if (ch == 3) { texel.b = applyLevels1(texel.b, RGB_Input_Black, RGB_Input_White, RGB_Gamma, RGB_Output_Black, RGB_Output_White, clipB, clipW); } else if (ch == 4) { texel.a = applyLevels1(texel.a, RGB_Input_Black, RGB_Input_White, RGB_Gamma, RGB_Output_Black, RGB_Output_White, clipB, clipW); } texel.r = applyLevels1(texel.r, Red_Input_Black, Red_Input_White, Red_Gamma, Red_Output_Black, Red_Output_White, clipB, clipW); texel.g = applyLevels1(texel.g, Green_Input_Black, Green_Input_White, Green_Gamma, Green_Output_Black, Green_Output_White, clipB, clipW); texel.b = applyLevels1(texel.b, Blue_Input_Black, Blue_Input_White, Blue_Gamma, Blue_Output_Black, Blue_Output_White, clipB, clipW); texel.a = applyLevels1(texel.a, Alpha_Input_Black, Alpha_Input_White, Alpha_Gamma, Alpha_Output_Black, Alpha_Output_White, clipB, clipW); float a = clamp(texel.a, 0.0, 1.0); float blurAmount = 1.0 - a; vec3 blurredRGB = blur9(vUvScaled, 6.0 * blurAmount); texel.rgb = mix(texel.rgb, blurredRGB, blurAmount); texel.a = a; gl_FragColor = texel; }
