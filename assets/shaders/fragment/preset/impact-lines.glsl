precision highp float; precision highp int; uniform sampler2D tDiffuse; uniform vec2 uvScale; uniform vec2 resolution; varying vec2 vUv; varying vec2 vUvScaled; uniform float time; uniform float Mode; uniform vec2 Center; uniform float Lines; uniform float Center_Gap; uniform float Outer_Radius; uniform float Radial_Expansion; uniform float Radial_Y_Scale; uniform float Speed_Line_Length; uniform vec3 Color; uniform float Opacity; uniform float Thickness; uniform float Taper; uniform float Start_Taper; uniform float End_Taper; uniform float Line_Style; uniform float Rough_Edge; uniform float Style_Count; uniform float Style_Amount; uniform float Variation; uniform float Animate; uniform float Auto_Animation; uniform float Direction; uniform float Grow_Phase; uniform float Flow_Phase; uniform float Speed; uniform float Speed_Variation; uniform float Flow_Life_Time; uniform float Flow_Length; uniform float Flow_Min_Scale; uniform float Phase_Randomness; uniform float Rotation; uniform float Length_Origin; uniform float Angle_Range; uniform float Angle_Offset; uniform float Fade_Start; uniform float Fade_End; uniform float Cap_Style; uniform float Dash_Length; uniform float Dash_Gap; uniform float Dash_Gap_Randomness; uniform float Angle_Randomness; uniform float Length_Randomness; uniform float Start_Position_Randomness; uniform float Thickness_Randomness; uniform float Opacity_Randomness; uniform float Speed_Spacing_Randomness; uniform float Speed_Position_Randomness; uniform float Color_Randomness; uniform float Seed; uniform float Rotation_Randomness; uniform float Flow_Fade; uniform float Depth_Curve; uniform float FPS; uniform float Evolution_Enable; uniform float Auto_Evolution; uniform float Auto_Evolution_Speed; uniform float Evolution; uniform float Evolution_Amount; uniform float Evolution_Speed; uniform float Evolution_Angle; uniform float Evolution_Rotation; uniform float Evolution_Rotation_Randomness; uniform float Evolution_Length; uniform float Evolution_Thickness; uniform float Evolution_Opacity; uniform float Output_Mode; uniform float Blend_Mode; float hash(float n) { return fract(sin(n) * 43758.5453123); } float hash(vec2 p) { return fract(sin(dot(p, vec2(127.1, 311.7))) * 43758.5453); } float hash(vec3 p) { return fract(sin(dot(p, vec3(127.1, 311.7, 74.7))) * 43758.5453); } float sdSegmentRound(vec2 p, vec2 a, vec2 b) { vec2 pa = p - a; vec2 ba = b - a; float h = clamp(dot(pa, ba) / dot(ba, ba), 0.0, 1.0); return length(pa - ba * h); } void main() { vec2 px = vUv * resolution; vec4 inputColor = texture2D(tDiffuse, vUvScaled); float t = time; if (FPS > 1.0) { t = floor(time * FPS) / FPS; } float evoTime = Evolution; if (Evolution_Enable > 0.5 && Auto_Evolution > 0.5) { evoTime += t * Auto_Evolution_Speed; } float evoVal = evoTime * Evolution_Speed; float evoAmt = clamp(Evolution_Amount / 100.0, 0.0, 1.0); vec4 lineAccum = vec4(0.0); if (Blend_Mode > 1.5) lineAccum = vec4(1.0, 1.0, 1.0, 0.0); int lineCount = int(min(Lines, 300.0)); float varNorm = clamp(Variation / 100.0, 0.0, 1.0); for (int i = 0; i < 300; i++) { if (i >= lineCount) break; float fi = float(i); float lineSeed = Seed + fi * 137.0 + floor(evoTime * 0.01) * 0.0; vec2 p1, p2; float baseAngle = 0.0; vec2 lineDir = vec2(1.0, 0.0); vec2 linePerp = vec2(0.0, 1.0); float lineLenEst = 100.0; if (Mode < 0.5) { if (Lines > 1.5) { float tNorm = fi / (Lines - 1.0); baseAngle = (Angle_Offset + (tNorm - 0.5) * Angle_Range) * 0.01745329252; } else { baseAngle = Angle_Offset * 0.01745329252; } baseAngle += Rotation * 0.01745329252; baseAngle += (hash(lineSeed + 1.0) - 0.5) * varNorm * 1.0; baseAngle += (hash(lineSeed + 10.0) - 0.5) * (Angle_Randomness / 100.0) * 1.57079632679; baseAngle += (hash(lineSeed + 11.0) - 0.5) * (Rotation_Randomness / 100.0) * 1.57079632679; if (Evolution_Enable > 0.5) { baseAngle += sin(fi * 0.5 + evoVal) * (Evolution_Angle / 100.0) * evoAmt * 1.57079632679; baseAngle += evoVal * Evolution_Rotation * 0.01745329252; baseAngle += (hash(lineSeed + 100.0 + evoTime * 0.1) - 0.5) * (Evolution_Rotation_Randomness / 100.0) * 6.28318530718 * evoAmt; } lineDir = vec2(cos(baseAngle), sin(baseAngle)); linePerp = vec2(-lineDir.y, lineDir.x); float ratio = Outer_Radius / max(Center_Gap, 0.001); float innerR = Center_Gap + Radial_Expansion; float outerR = innerR * ratio; vec2 scaleVec = vec2(1.0, Radial_Y_Scale); float lenRand = hash(lineSeed + 20.0); outerR *= mix(1.0, 0.2 + lenRand * 1.8, Length_Randomness / 100.0); if (Evolution_Enable > 0.5) { outerR *= mix(1.0, 0.5 + 0.5 * sin(evoVal * 1.1 + fi * 0.7), (Evolution_Length / 100.0) * evoAmt); } p1 = Center + lineDir * innerR * scaleVec; p2 = Center + lineDir * outerR * scaleVec; float startPosRand = (hash(lineSeed + 30.0) - 0.5) * Start_Position_Randomness; p1 += lineDir * startPosRand * scaleVec; lineLenEst = length(p2 - p1); } else { float speedAngle = (Rotation + Angle_Offset) * 0.01745329252; lineDir = vec2(cos(speedAngle), sin(speedAngle)); linePerp = vec2(-lineDir.y, lineDir.x); float perpPos = 0.0; if (Lines > 1.5) { perpPos = (fi / (Lines - 1.0) - 0.5) * max(resolution.x, resolution.y) * 1.5; } perpPos += (hash(lineSeed + 2.0) - 0.5) * Speed_Position_Randomness; perpPos += hash(lineSeed + 3.0) * Speed_Spacing_Randomness; perpPos += (hash(lineSeed + 4.0) - 0.5) * varNorm * 200.0; float sLen = Speed_Line_Length; if (sLen < 1.0) sLen = max(resolution.x, resolution.y) * 1.5; sLen *= mix(1.0, 0.3 + hash(lineSeed + 20.0) * 1.4, Length_Randomness / 100.0); if (Evolution_Enable > 0.5) { sLen *= mix(1.0, 0.5 + 0.5 * sin(evoVal * 1.1 + fi * 0.7), (Evolution_Length / 100.0) * evoAmt); } vec2 lineCenter = Center + linePerp * perpPos; float flowOffset = 0.0; if (abs(Animate - 3.0) < 0.5 || abs(Animate - 2.0) < 0.5) { float phase = Flow_Phase; if (Auto_Animation > 0.5) phase += t * Speed; float lineSpeed = Speed; if (Speed_Variation > 0.0) { lineSpeed *= mix(1.0, 0.3 + hash(lineSeed + 800.0) * 1.4, Speed_Variation / 100.0); } float moveDist = phase * lineSpeed * 300.0; if (Direction > 0.5) moveDist = -moveDist; float wrapWidth = sLen * 3.0; flowOffset = mod(moveDist + fi * (wrapWidth / max(Lines, 1.0)) + hash(lineSeed + 5.0) * wrapWidth, wrapWidth) - wrapWidth * 0.5; } lineCenter += lineDir * flowOffset; p1 = lineCenter - lineDir * sLen * 0.5; p2 = lineCenter + lineDir * sLen * 0.5; lineLenEst = sLen; } if (abs(Animate - 1.0) < 0.5) { float phase = clamp(Grow_Phase, 0.0, 1.0); if (Auto_Animation > 0.5) { phase = fract(t * Speed); } vec2 fullP1 = p1; vec2 fullP2 = p2; vec2 mid = (fullP1 + fullP2) * 0.5; vec2 halfVec = (fullP2 - fullP1) * 0.5; if (Direction < 0.5) { if (Length_Origin < 0.5) { p2 = mix(fullP1, fullP2, phase); } else if (abs(Length_Origin - 1.0) < 0.5) { p1 = mid - halfVec * phase; p2 = mid + halfVec * phase; } else { p1 = mix(fullP2, fullP1, 1.0 - phase); } } else { if (Length_Origin < 0.5) { p1 = mix(fullP2, fullP1, 1.0 - phase); } else if (abs(Length_Origin - 1.0) < 0.5) { p1 = mid + halfVec * phase; p2 = mid - halfVec * phase; } else { p2 = mix(fullP1, fullP2, 1.0 - phase); } } } vec2 ba = p2 - p1; float segLen = length(ba); if (segLen < 0.001) continue; vec2 dirNorm = ba / segLen; vec2 perpNorm = vec2(-dirNorm.y, dirNorm.x); vec2 pa = px - p1; float tProj = clamp(dot(pa, dirNorm) / segLen, 0.0, 1.0); vec2 closest = p1 + dirNorm * segLen * tProj; float dist = length(px - closest); float bestT = tProj; int samples = 1; if (abs(Line_Style - 1.0) < 0.5) samples = int(clamp(Style_Count * 3.0, 4.0, 40.0)); else if (abs(Line_Style - 2.0) < 0.5) samples = 24; else if (Rough_Edge > 0.01) samples = 12; if (samples > 1) { float minD = dist; for (int s = 0; s < 40; s++) { if (s >= samples) break; float ts = float(s) / max(float(samples - 1), 1.0); vec2 pos = p1 + dirNorm * segLen * ts; if (abs(Line_Style - 1.0) < 0.5) { float phase = ts * max(Style_Count, 1.0) * 6.28318530718; float wave = asin(sin(phase)) / 1.57079632679 * Style_Amount; pos += perpNorm * wave; } else if (abs(Line_Style - 2.0) < 0.5) { float wave = sin(ts * max(Style_Count, 1.0) * 6.28318530718) * Style_Amount; pos += perpNorm * wave; } float d = length(px - pos); if (d < minD) { minD = d; bestT = ts; } } dist = minD; } if (Rough_Edge > 0.01) { float edgeNoise = (hash(vec2(bestT * 40.0, lineSeed + 200.0)) - 0.5) * Rough_Edge * Thickness * 3.0; dist += edgeNoise; } float thick = Thickness; thick *= mix(1.0, sin(bestT * 3.14159265), clamp(Taper, 0.0, 1.0)); thick *= mix(1.0, smoothstep(0.0, 0.3, bestT), clamp(Start_Taper, 0.0, 1.0)); thick *= mix(1.0, smoothstep(1.0, 0.7, bestT), clamp(End_Taper, 0.0, 1.0)); if (Evolution_Enable > 0.5) { thick *= mix(1.0, 0.5 + 0.5 * sin(evoVal * 1.1 + fi * 0.5), (Evolution_Thickness / 100.0) * evoAmt); } thick *= mix(1.0, 0.2 + hash(lineSeed + 400.0) * 1.8, Thickness_Randomness / 100.0); if (Depth_Curve > 0.0 && Mode < 0.5) { float depth = mix(1.0, 1.0 - bestT * 0.7, Depth_Curve); thick *= depth; } if (Cap_Style < 0.5) { float projT = dot(px - p1, dirNorm) / segLen; if (projT < 0.0 || projT > 1.0) { dist = 1e10; } } if (dist > thick * 0.6) continue; float inDash = 1.0; if (Dash_Length > 0.5 && Dash_Gap > 0.5) { float dashCycle = Dash_Length + Dash_Gap; float dashRand = hash(lineSeed + 300.0) * Dash_Gap_Randomness; float dashPos = mod(bestT * segLen + dashRand, dashCycle); inDash = step(dashPos, Dash_Length); } if (inDash < 0.5) continue; float flowAlpha = 1.0; if (abs(Animate - 2.0) < 0.5) { float phase = Flow_Phase; if (Auto_Animation > 0.5) phase += t * Speed; float lineSpeed = Speed; if (Speed_Variation > 0.0) { lineSpeed *= mix(1.0, 0.3 + hash(lineSeed + 800.0) * 1.4, Speed_Variation / 100.0); } float phaseRand = Phase_Randomness / 100.0; float linePhase = phase * lineSpeed + hash(lineSeed + 40.0) * phaseRand * 6.28318530718; float flowPos = bestT * segLen; if (Mode < 0.5 && Direction > 0.5) { flowPos = segLen - flowPos; } float segLenFlow = max(Flow_Length, 5.0); float segSpacing = segLenFlow * 2.5; float localPos = mod(flowPos - linePhase * 80.0 + hash(lineSeed + 60.0) * segSpacing, segSpacing); float seg = smoothstep(segLenFlow, segLenFlow * 0.7, localPos) * smoothstep(0.0, segLenFlow * 0.15, localPos); if (Flow_Min_Scale > 0.0) { seg *= mix(1.0, smoothstep(segLenFlow * Flow_Min_Scale, 0.0, localPos), clamp(Flow_Min_Scale, 0.0, 1.0)); } if (Flow_Life_Time > 0.0) { float life = mod(linePhase, Flow_Life_Time * 3.0) / max(Flow_Life_Time * 3.0, 0.001); seg *= smoothstep(1.0, 0.3, life); } flowAlpha = seg; } float alpha = Opacity; alpha *= smoothstep(0.0, max(Fade_Start, 0.5), bestT * segLen); alpha *= smoothstep(0.0, max(Fade_End, 0.5), (1.0 - bestT) * segLen); if (abs(Animate - 2.0) < 0.5 && Flow_Fade > 0.0) { alpha *= mix(1.0, smoothstep(1.0, 0.0, bestT), clamp(Flow_Fade, 0.0, 1.0)); } if (Evolution_Enable > 0.5) { alpha *= mix(1.0, 0.5 + 0.5 * sin(evoVal * 1.3 + fi * 0.7), (Evolution_Opacity / 100.0) * evoAmt); } alpha *= mix(1.0, hash(lineSeed + 500.0), Opacity_Randomness / 100.0); alpha *= mix(1.0, 0.3 + hash(lineSeed + 501.0) * 1.4, varNorm * 0.5); alpha *= flowAlpha; float edgeSoft = smoothstep(thick * 0.5, thick * 0.25, dist); alpha *= edgeSoft; if (alpha < 0.001) continue; vec3 lineColor = Color; if (Color_Randomness > 0.0) { vec3 randCol = vec3( hash(lineSeed + 600.0), hash(lineSeed + 601.0), hash(lineSeed + 602.0) ); lineColor = mix(lineColor, randCol, Color_Randomness / 100.0); } vec4 lc = vec4(lineColor * alpha, alpha); if (Blend_Mode < 0.5) { lineAccum.rgb = mix(lineAccum.rgb, lc.rgb, lc.a); lineAccum.a = lineAccum.a + lc.a * (1.0 - lineAccum.a); } else if (Blend_Mode > 0.5) { lineAccum += lc; } else { lineAccum.rgb *= mix(vec3(1.0), lc.rgb, lc.a); lineAccum.a = max(lineAccum.a, lc.a); } } lineAccum = clamp(lineAccum, 0.0, 1.0); vec4 finalColor; if (Output_Mode < 0.5) { if (Blend_Mode > 0.5) { finalColor = inputColor + lineAccum; finalColor.a = min(inputColor.a + lineAccum.a, 1.0); } else if (Blend_Mode > 1.5) { finalColor.rgb = inputColor.rgb * mix(vec3(1.0), lineAccum.rgb, lineAccum.a); finalColor.a = inputColor.a; } else { finalColor = mix(inputColor, lineAccum, lineAccum.a); } } else { finalColor = lineAccum; } gl_FragColor = finalColor; }
