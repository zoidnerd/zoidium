precision highp float; precision highp int; uniform sampler2D tDiffuse; uniform vec2 resolution; varying vec2 vUvScaled; uniform float Invert; uniform float Blend_With_Original; const float EDGE_NORM = 1.05; const float GAMMA = 0.75; vec3 toLinear(vec3 c) { return pow(c, vec3(GAMMA)); } vec3 toSRGB (vec3 c) { return pow(c, vec3(1.0 / GAMMA)); } void main() { vec4 src = texture2D(tDiffuse, vUvScaled); vec2 px = 1.0 / resolution; vec3 tl = toLinear(texture2D(tDiffuse, vUvScaled + vec2(-px.x, -px.y)).rgb); vec3 tc = toLinear(texture2D(tDiffuse, vUvScaled + vec2( 0.0 , -px.y)).rgb); vec3 tr = toLinear(texture2D(tDiffuse, vUvScaled + vec2( px.x , -px.y)).rgb); vec3 ml = toLinear(texture2D(tDiffuse, vUvScaled + vec2(-px.x, 0.0 )).rgb); vec3 mr = toLinear(texture2D(tDiffuse, vUvScaled + vec2( px.x , 0.0 )).rgb); vec3 bl = toLinear(texture2D(tDiffuse, vUvScaled + vec2(-px.x, px.y)).rgb); vec3 bc = toLinear(texture2D(tDiffuse, vUvScaled + vec2( 0.0 , px.y)).rgb); vec3 br = toLinear(texture2D(tDiffuse, vUvScaled + vec2( px.x , px.y)).rgb); vec3 gx = (-1.0*tl) + ( 1.0*tr) + (-2.0*ml) + ( 2.0*mr) + (-1.0*bl) + ( 1.0*br); vec3 gy = (-1.0*tl) + (-2.0*tc) + (-1.0*tr) + (1.0*bl) + ( 2.0*bc) + ( 1.0*br); vec3 edgeLin = clamp(sqrt(gx*gx + gy*gy) / EDGE_NORM, 0.0, 1.0); vec3 effectLin = mix(vec3(1.0) - edgeLin, edgeLin, step(0.5, Invert)); vec3 effectSRGB = toSRGB(effectLin); float bwo = clamp(Blend_With_Original, 0.0, 100.0) * 0.01; vec3 outRGB = mix(effectSRGB, src.rgb, bwo); gl_FragColor = vec4(outRGB, src.a); }
