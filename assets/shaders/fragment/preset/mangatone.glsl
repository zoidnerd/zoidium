precision highp float; precision highp int; uniform sampler2D tDiffuse; uniform vec2 uvScale; uniform vec2 resolution; varying vec2 vUvScaled; varying vec2 vUv; uniform float Contrast; uniform float Tone_Curve_Gamma; uniform int Outline_Enable; uniform float Outline_Threshold; uniform float Outline_Strength; uniform float Outline_Fill_Radius; uniform float Lines_Dark_Fill_Threshold; uniform int Pattern_Type; uniform float Tone_Levels; uniform int Solid_Black_Enable; uniform float Solid_Black_Threshold; uniform int Solid_White_Enable; uniform float Solid_White_Threshold; uniform int Gradient_Boundaries; uniform float Gradient_Smoothness; uniform float Pattern_Size; uniform float Tone_Rotation; uniform int Background_Alpha; uniform float Background_Opacity; uniform int Alpha_Ink_Color; uniform float Invert_Pattern_Enable; uniform float Invert_Pattern_Threshold; uniform float Tone_Coeff_1; uniform float Upper_Boundary_1; uniform float Tone_Coeff_2; uniform float Upper_Boundary_2; uniform float Tone_Coeff_3; uniform float Upper_Boundary_3; uniform float Tone_Coeff_4; float luminance(vec3 color) { return dot(color, vec3(0.299, 0.587, 0.114)); } mat2 rotate2d(float _angle) { return mat2(cos(_angle), -sin(_angle), sin(_angle), cos(_angle)); } float sobelEdge(sampler2D tex, vec2 uv, vec2 res, float radius) { vec2 step = radius / res; float tL = luminance(texture2D(tex, uv + vec2(-step.x, step.y)).rgb); float tC = luminance(texture2D(tex, uv + vec2(0.0, step.y)).rgb); float tR = luminance(texture2D(tex, uv + vec2(step.x, step.y)).rgb); float mL = luminance(texture2D(tex, uv + vec2(-step.x, 0.0)).rgb); float mR = luminance(texture2D(tex, uv + vec2(step.x, 0.0)).rgb); float bL = luminance(texture2D(tex, uv + vec2(-step.x, -step.y)).rgb); float bC = luminance(texture2D(tex, uv + vec2(0.0, -step.y)).rgb); float bR = luminance(texture2D(tex, uv + vec2(step.x, -step.y)).rgb); float x = tL + 2.0*mL + bL - tR - 2.0*mR - bR; float y = tL + 2.0*tC + tR - bL - 2.0*bC - bR; return sqrt(x*x + y*y); } float getPattern(vec2 uv, int type, float size, float angle) { vec2 st = uv; float aspect = resolution.x / resolution.y; st.x *= aspect; st -= vec2(0.5 * aspect, 0.5); st *= rotate2d(radians(angle)); st += vec2(0.5 * aspect, 0.5); st *= size; if (type == 0) { vec2 grid = fract(st) - 0.5; float dist = length(grid); return 1.0 - (dist * 2.0); } else { float line = fract(st.y); return 1.0 - abs((line - 0.5) * 2.0); } } void main() { vec4 texel = texture2D(tDiffuse, vUvScaled); vec3 color = texel.rgb; float alpha = texel.a; color = (color - 0.5) * max(Contrast, 0.0) + 0.5; color = pow(abs(color), vec3(1.0 / max(Tone_Curve_Gamma, 0.01))); float lum = luminance(color); float edge = 0.0; if (Outline_Enable == 1) { edge = sobelEdge(tDiffuse, vUvScaled, resolution, max(Outline_Fill_Radius, 1.0)); edge *= Outline_Strength; edge = step(Outline_Threshold, edge); if (lum < Lines_Dark_Fill_Threshold) { edge = 1.0; } } float finalTone = 1.0; if (edge < 1.0) { float stageValue = 0.0; float targetTone = 1.0; if (lum <= Upper_Boundary_1) { targetTone = Tone_Coeff_1; } else if (lum <= Upper_Boundary_2) { targetTone = Tone_Coeff_2; } else if (lum <= Upper_Boundary_3) { targetTone = Tone_Coeff_3; } else { targetTone = Tone_Coeff_4; } if (Gradient_Boundaries == 1 && Gradient_Smoothness > 0.0) { float smoothRange = Gradient_Smoothness * 0.1; if (abs(lum - Upper_Boundary_1) < smoothRange) { float t = smoothstep(Upper_Boundary_1 - smoothRange, Upper_Boundary_1 + smoothRange, lum); targetTone = mix(Tone_Coeff_1, Tone_Coeff_2, t); } else if (abs(lum - Upper_Boundary_2) < smoothRange) { float t = smoothstep(Upper_Boundary_2 - smoothRange, Upper_Boundary_2 + smoothRange, lum); targetTone = mix(Tone_Coeff_2, Tone_Coeff_3, t); } else if (abs(lum - Upper_Boundary_3) < smoothRange) { float t = smoothstep(Upper_Boundary_3 - smoothRange, Upper_Boundary_3 + smoothRange, lum); targetTone = mix(Tone_Coeff_3, Tone_Coeff_4, t); } } float patternVal = getPattern(vUv, Pattern_Type, Pattern_Size, Tone_Rotation); if (Tone_Levels > 0.0) { patternVal = floor(patternVal * Tone_Levels) / Tone_Levels; } bool invertPattern = (Invert_Pattern_Enable > 0.5 && lum < Invert_Pattern_Threshold); if (invertPattern) { if (patternVal > (1.0 - targetTone)) { finalTone = 1.0; } else { finalTone = 0.0; } } else { if (patternVal > targetTone) { finalTone = 0.0; } else { finalTone = 1.0; } } if (Solid_Black_Enable == 1 && lum <= Solid_Black_Threshold) { finalTone = 0.0; } if (Solid_White_Enable == 1 && lum >= Solid_White_Threshold) { finalTone = 1.0; } } else { finalTone = 0.0; } vec4 outputColor = vec4(vec3(finalTone), 1.0); if (alpha < 1.0) { if (Background_Alpha == 1) { outputColor.a = Background_Opacity; } else { if (Alpha_Ink_Color == 0) { outputColor.a = alpha; } else if (Alpha_Ink_Color == 1) { outputColor = vec4(0.0, 0.0, 0.0, 1.0); } else if (Alpha_Ink_Color == 2) { outputColor = vec4(1.0, 1.0, 1.0, 1.0); } } } gl_FragColor = outputColor; }
