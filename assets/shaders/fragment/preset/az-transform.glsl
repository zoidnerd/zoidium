precision highp float; precision highp int; uniform sampler2D tDiffuse; uniform vec2 resolution; varying vec2 vUvScaled; uniform vec2 Anchor_Point; uniform vec2 Position; uniform float Uniform_Scale; uniform float Scale_Height; uniform float Scale_Width; uniform float Skew; uniform float Skew_Axis; uniform float Rotation; uniform float Opacity; uniform float Use_Composition_Shutter_Angle; uniform float Shutter_Angle; uniform float Sampling; float pctToFactor(float pct) { return pct * 0.01; } float mnWeight(float x) { const float B = 1.0/3.0; const float C = 1.0/3.0; float ax = abs(x); float ax2 = ax * ax; float ax3 = ax2 * ax; if (ax < 1.0) { return ((12.0 - 9.0*B - 6.0*C) * ax3 + (-18.0 + 12.0*B + 6.0*C) * ax2 + (6.0 - 2.0*B)) * ax; } else if (ax < 2.0) { return ((-B - 6.0*C) * ax3 + (6.0*B + 30.0*C) * ax2 + (-12.0*B - 48.0*C) * ax + (8.0*B + 24.0*C)); } return 0.0; } vec4 sampleTexBilinear(vec2 uv01) { if (uv01.x < 0.0 || uv01.x > 1.0 || uv01.y < 0.0 || uv01.y > 1.0) return vec4(0.0); return texture2D(tDiffuse, uv01); } vec4 sampleBicubic(sampler2D tex, vec2 uv, vec2 texSize) { vec2 xy = uv * texSize - 0.5; vec2 ixy = floor(xy); vec2 fxy = xy - ixy; vec4 col = vec4(0.0); float wSum = 0.0; for (int j = -1; j <= 2; ++j) { float wy = mnWeight(float(j) - fxy.y); float y = (ixy.y + float(j)) / texSize.y; for (int i = -1; i <= 2; ++i) { float wx = mnWeight(float(i) - fxy.x); float x = (ixy.x + float(i)) / texSize.x; float w = wx * wy; col += texture2D(tex, vec2(x, y)) * w; wSum += w; } } return col / wSum; } vec4 sampleTexHQ(vec2 uv01) { if (uv01.x < 0.0 || uv01.x > 1.0 || uv01.y < 0.0 || uv01.y > 1.0) return vec4(0.0); return sampleBicubic(tDiffuse, uv01, resolution); } vec2 inverseMapPixel_AE( vec2 P_out, vec2 anchor, vec2 position, float scaleH_pct, float scaleW_pct, float uniformScale, float skewDeg, float skewAxisDeg, float rotationDeg ) { float sY = pctToFactor(scaleH_pct); float sX = (uniformScale >= 0.5) ? sY : pctToFactor(scaleW_pct); bool noRot = abs(rotationDeg) < 1e-6; bool noSkew = abs(skewDeg) < 1e-6; bool unitScale = (abs(sX - 1.0) < 1e-6) && (abs(sY - 1.0) < 1e-6); if (noRot && noSkew && unitScale) { return P_out - position + anchor; } vec2 p = P_out - position; float rr = radians(-rotationDeg); float cr = cos(rr), sr = sin(rr); p = vec2(cr*p.x - sr*p.y, sr*p.x + cr*p.y); if (!noSkew) { float k = -tan(radians(skewDeg)); float aa = radians(skewAxisDeg); float ca = cos(aa), sa = sin(aa); vec2 q = vec2( ca*p.x + sa*p.y, -sa*p.x + ca*p.y ); q.x = q.x + k * q.y; p = vec2( ca*q.x - sa*q.y, sa*q.x + ca*q.y ); } float inv_sX = (abs(sX) > 1e-6) ? (1.0 / sX) : 0.0; float inv_sY = (abs(sY) > 1e-6) ? (1.0 / sY) : 0.0; p *= vec2(inv_sX, inv_sY); return p + anchor; } void main() { float op = clamp(Opacity * 0.01, 0.0, 1.0); if (op <= 0.00001) { gl_FragColor = vec4(0.0); return; } vec2 P_out = vUvScaled * resolution; vec2 P_in = inverseMapPixel_AE( P_out, Anchor_Point, Position, Scale_Height, Scale_Width, Uniform_Scale, Skew, Skew_Axis, Rotation ); vec2 uv_src = P_in / resolution; vec4 color = (Sampling >= 0.5) ? sampleTexHQ(uv_src) : sampleTexBilinear(uv_src); color.rgb *= op; color.a *= op; gl_FragColor = color; }
