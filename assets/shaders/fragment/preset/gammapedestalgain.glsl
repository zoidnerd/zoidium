precision highp float; precision highp int; uniform sampler2D tDiffuse; varying vec2 vUvScaled; uniform float Black_Stretch; uniform float Red_Gamma; uniform float Red_Pedestal; uniform float Red_Gain; uniform float Green_Gamma; uniform float Green_Pedestal; uniform float Green_Gain; uniform float Blue_Gamma; uniform float Blue_Pedestal; uniform float Blue_Gain; const float EPS = 0.00001; const vec3 LUMA = vec3(0.2126, 0.7152, 0.0722); float safeGamma(float g) { return max(g, EPS); } float blackStretchCurve(float x, float stretch) { x = clamp(x, 0.0, 1.0); stretch = max(stretch, EPS); float lifted = 1.0 - pow(1.0 - x, stretch); float shadowMask = pow(1.0 - x, 2.0); return mix(x, lifted, shadowMask); } float applyChannelGPG(float x, float gammaVal, float pedestal, float gainVal) { x = clamp(x, 0.0, 1.0); float curved = pow(x, 1.0 / safeGamma(gammaVal)); float y = pedestal + (gainVal - pedestal) * curved; return clamp(y, 0.0, 1.0); } void main() { vec4 texel = texture2D(tDiffuse, vUvScaled); vec3 c = clamp(texel.rgb, 0.0, 1.0); float y = dot(c, LUMA); float y2 = blackStretchCurve(y, Black_Stretch); float scale = y2 / max(y, EPS); c *= scale; c = clamp(c, 0.0, 1.0); c.r = applyChannelGPG(c.r, Red_Gamma, Red_Pedestal, Red_Gain); c.g = applyChannelGPG(c.g, Green_Gamma, Green_Pedestal, Green_Gain); c.b = applyChannelGPG(c.b, Blue_Gamma, Blue_Pedestal, Blue_Gain); gl_FragColor = vec4(c, texel.a); }
