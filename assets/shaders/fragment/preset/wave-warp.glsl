precision highp float; precision highp int; uniform sampler2D tDiffuse; uniform vec2 uvScale; uniform vec2 resolution; varying vec2 vUv; uniform float Wave_Type; uniform float Wave_Height; uniform float Wave_Width; uniform float Direction; uniform float Wave_Speed; uniform float Pinning; uniform float Phase; uniform float Antialiasing; uniform float time; const float PI = 3.14159265358979323846; const float TAU = 6.28318530717958647692; float hash11(float p) { p = fract(p * 0.1031); p *= p + 33.33; p *= p + p; return fract(p); } float noise1D_step(float x) { float i = floor(x); return hash11(i) * 2.0 - 1.0; } float noise1D_smooth(float x) { float i = floor(x); float f = fract(x); float a = hash11(i); float b = hash11(i + 1.0); float u = f * f * (3.0 - 2.0 * f); return (mix(a, b, u) * 2.0 - 1.0); } float waveShape(float ph, int wType, int aa, float coordForNoise) { float s = sin(ph); float u = fract(ph / TAU); float k = 0.0; if (aa == 1) k = 0.015; if (aa == 2) k = 0.030; if (wType == 0) { return s; } else if (wType == 1) { if (aa == 0) return sign(s); return smoothstep(-k, k, s) * 2.0 - 1.0; } else if (wType == 2) { return (2.0 / PI) * asin(clamp(s, -1.0, 1.0)); } else if (wType == 3) { return u * 2.0 - 1.0; } else if (wType == 4) { float halfSign = (u < 0.5) ? 1.0 : -1.0; float uh = fract(u * 2.0); float x = uh * 2.0 - 1.0; float y = sqrt(max(0.0, 1.0 - x*x)); return y * halfSign; } else if (wType == 5) { float x = u * 2.0 - 1.0; float y = sqrt(max(0.0, 1.0 - x*x)); return y * 2.0 - 1.0; } else if (wType == 6) { float halfSign = (u < 0.5) ? 1.0 : -1.0; float uh = fract(u * 2.0); float x = uh * 2.0 - 1.0; float y = sqrt(max(0.0, 1.0 - x*x)); float inv = 1.0 - y; return (inv * 2.0 - 1.0) * halfSign; } else if (wType == 7) { return noise1D_step(coordForNoise); } else { return noise1D_smooth(coordForNoise); } } float pinFactor(vec2 uv, vec2 n, int pinMode) { if (pinMode == 0) return 1.0; float left = uv.x; float right = 1.0 - uv.x; float bottom = uv.y; float top = 1.0 - uv.y; if (pinMode == 1) { float e = min(min(left, right), min(top, bottom)); return clamp(e * 2.0, 0.0, 1.0); } if (pinMode == 3) return clamp(left, 0.0, 1.0); if (pinMode == 5) return clamp(right, 0.0, 1.0); if (pinMode == 6) return clamp(bottom, 0.0, 1.0); if (pinMode == 4) return clamp(top, 0.0, 1.0); if (pinMode == 7) { float e = min(top, bottom); return clamp(e * 2.0, 0.0, 1.0); } if (pinMode == 8) { float e = min(left, right); return clamp(e * 2.0, 0.0, 1.0); } vec2 p = uv * resolution; float proj = dot(p, n); float W = resolution.x; float H = resolution.y; float a = W * n.x; float b = H * n.y; float mn = min(0.0, min(a, min(b, a + b))); float mx = max(0.0, max(a, max(b, a + b))); float t = (mx > mn) ? ((proj - mn) / (mx - mn)) : 0.5; float d = abs(t - 0.5) * 2.0; return clamp(d, 0.0, 1.0); } vec4 sampleLayerAA(vec2 uv, int aa) { if (any(lessThan(uv, vec2(0.0))) || any(greaterThan(uv, vec2(1.0)))) { return vec4(0.0); } vec2 px = 0.5 / resolution; if (aa == 0) { return texture2D(tDiffuse, uv * uvScale); } if (aa == 1) { vec4 c0 = texture2D(tDiffuse, (uv + vec2(-px.x, -px.y)) * uvScale); vec4 c1 = texture2D(tDiffuse, (uv + vec2( px.x, -px.y)) * uvScale); vec4 c2 = texture2D(tDiffuse, (uv + vec2(-px.x, px.y)) * uvScale); vec4 c3 = texture2D(tDiffuse, (uv + vec2( px.x, px.y)) * uvScale); return (c0 + c1 + c2 + c3) * 0.25; } vec4 sum = vec4(0.0); sum += texture2D(tDiffuse, (uv + vec2(-px.x, -px.y)) * uvScale); sum += texture2D(tDiffuse, (uv + vec2( 0.0, -px.y)) * uvScale); sum += texture2D(tDiffuse, (uv + vec2( px.x, -px.y)) * uvScale); sum += texture2D(tDiffuse, (uv + vec2(-px.x, 0.0)) * uvScale); sum += texture2D(tDiffuse, (uv + vec2( 0.0, 0.0)) * uvScale); sum += texture2D(tDiffuse, (uv + vec2( px.x, 0.0)) * uvScale); sum += texture2D(tDiffuse, (uv + vec2(-px.x, px.y)) * uvScale); sum += texture2D(tDiffuse, (uv + vec2( 0.0, px.y)) * uvScale); sum += texture2D(tDiffuse, (uv + vec2( px.x, px.y)) * uvScale); return sum / 9.0; } void main() { vec2 uv = vUv; int wType = int(floor(Wave_Type + 0.5)); int pin = int(floor(Pinning + 0.5)); int aa = int(floor(Antialiasing + 0.5)); float wWidth = max(Wave_Width, 0.0001); float ang = radians(Direction); vec2 d = vec2(cos(ang), sin(ang)); vec2 n = vec2(-d.y, d.x); vec2 p = uv * resolution; float coord = dot(p, d); float phaseCycles = Phase / 360.0; float cycles = (coord / wWidth) + (time * Wave_Speed) + phaseCycles; float ph = cycles * TAU; float w = waveShape(ph, wType, aa, cycles); float pinF = pinFactor(uv, n, pin); float dispPx = w * Wave_Height * pinF; vec2 uvWarp = uv + (dispPx * n) / resolution; gl_FragColor = sampleLayerAA(uvWarp, aa); }
