precision highp float; precision highp int; uniform sampler2D tDiffuse; varying vec2 vUvScaled; uniform float Shadow_Red_Balance; uniform float Shadow_Green_Balance; uniform float Shadow_Blue_Balance; uniform float Midtone_Red_Balance; uniform float Midtone_Green_Balance; uniform float Midtone_Blue_Balance; uniform float Highlight_Red_Balance; uniform float Highlight_Green_Balance; uniform float Highlight_Blue_Balance; uniform float Preserve_Luminosity; const float RANGE_SCALE = 0.50; const vec3 LUMA = vec3(0.30, 0.59, 0.11); float lum(vec3 c) { return dot(c, LUMA); } vec3 clipColor(vec3 c) { float l = lum(c); float n = min(min(c.r, c.g), c.b); float x = max(max(c.r, c.g), c.b); if (n < 0.0) { float denom = max(l - n, 1e-5); c = vec3(l) + ((c - vec3(l)) * l) / denom; } if (x > 1.0) { float denom = max(x - l, 1e-5); c = vec3(l) + ((c - vec3(l)) * (1.0 - l)) / denom; } return clamp(c, 0.0, 1.0); } vec3 setLum(vec3 c, float targetLum) { float d = targetLum - lum(c); c += vec3(d); return clipColor(c); } void getToneWeights(float y, out float shadows, out float midtones, out float highlights) { const float a = 0.25; const float b = 0.333; const float s = 0.70; shadows = clamp((y - b) / (-a) + 0.5, 0.0, 1.0) * s; midtones = clamp((y - b) / a + 0.5, 0.0, 1.0) * clamp((y + b - 1.0) / (-a) + 0.5, 0.0, 1.0) * s; highlights = clamp((y + b - 1.0) / a + 0.5, 0.0, 1.0) * s; float sumW = shadows + midtones + highlights; if (sumW > 1e-5) { shadows /= sumW; midtones /= sumW; highlights /= sumW; } } vec3 sliderToDelta(vec3 slider) { return slider * (RANGE_SCALE / 100.0); } void main() { vec4 texel = texture2D(tDiffuse, vUvScaled); vec3 src = texel.rgb; float y = lum(src); float wShadows; float wMidtones; float wHighlights; getToneWeights(y, wShadows, wMidtones, wHighlights); vec3 sh = sliderToDelta(vec3( Shadow_Red_Balance, Shadow_Green_Balance, Shadow_Blue_Balance )); vec3 mi = sliderToDelta(vec3( Midtone_Red_Balance, Midtone_Green_Balance, Midtone_Blue_Balance )); vec3 hi = sliderToDelta(vec3( Highlight_Red_Balance, Highlight_Green_Balance, Highlight_Blue_Balance )); vec3 delta = sh * wShadows + mi * wMidtones + hi * wHighlights; vec3 balanced = src + delta; balanced = clamp(balanced, 0.0, 1.0); if (Preserve_Luminosity > 0.5) { balanced = setLum(balanced, lum(src)); } gl_FragColor = vec4(clamp(balanced, 0.0, 1.0), texel.a); }
