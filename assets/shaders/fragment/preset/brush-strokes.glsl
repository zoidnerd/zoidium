precision highp float; precision highp int; uniform sampler2D tDiffuse; varying vec2 vUvScaled; varying vec2 vUv; uniform vec2 resolution; uniform vec2 uvScale; uniform float Stroke_Angle; uniform float Brush_Size; uniform float Stroke_Length; uniform float Stroke_Density; uniform float Stroke_Randomness; uniform float Paint_Surface; uniform float Blend_With_Original; float hash(vec2 p) { return fract(sin(dot(p, vec2(12.9898, 78.233))) * 43758.5453123); } vec2 hash2(vec2 p) { return fract(sin(vec2(dot(p, vec2(127.1, 311.7)), dot(p, vec2(269.5, 183.3)))) * 43758.5453); } void main() { vec4 originalColor = texture2D(tDiffuse, vUvScaled); if (Brush_Size <= 0.5 || Stroke_Length <= 0.01) { gl_FragColor = mix(originalColor, originalColor, clamp(Blend_With_Original / 100.0, 0.0, 1.0)); return; } int surfaceMode = int(Paint_Surface + 0.5); vec4 bgColor = originalColor; if (surfaceMode == 1) bgColor = vec4(0.0); else if (surfaceMode == 2) bgColor = vec4(1.0); else if (surfaceMode == 3) bgColor = vec4(0.0, 0.0, 0.0, 1.0); float angle = radians(Stroke_Angle); float s = sin(angle); float c = cos(angle); mat2 rot = mat2(c, -s, s, c); mat2 invRot = mat2(c, s, -s, c); vec2 pos = vUv * resolution; vec2 rotatedPos = rot * pos; float densityFactor = max(0.1, Stroke_Density); float spacing = max(1.0, Brush_Size / densityFactor); float halfWidth = max(1.0, Brush_Size * 0.5); float halfLength = max(1.0, halfWidth * Stroke_Length); vec4 paintedColor = vec4(0.0); float highestZ = -1.0; float maxSearchY = ceil((halfLength / spacing) + 1.0); float maxSearchX = ceil((halfWidth / spacing) + 1.0); for (float y = -20.0; y <= 20.0; y++) { if (abs(y) > maxSearchY) continue; for (float x = -3.0; x <= 3.0; x++) { if (abs(x) > maxSearchX) continue; vec2 gridPos = floor(rotatedPos / spacing) + vec2(x, y); vec2 cellCenter = (gridPos + 0.5) * spacing; vec2 randomOffset = (hash2(gridPos) - 0.5) * spacing * Stroke_Randomness * 1.5; vec2 strokeCenter = cellCenter + randomOffset; vec2 delta = rotatedPos - strokeCenter; float distSq = (delta.x * delta.x) / (halfWidth * halfWidth) + (delta.y * delta.y) / (halfLength * halfLength); if (distSq <= 1.0) { float alpha = smoothstep(1.0, 0.7, distSq); vec2 samplePos = invRot * strokeCenter; vec2 sampleUv = samplePos / resolution; if (sampleUv.x >= 0.0 && sampleUv.x <= 1.0 && sampleUv.y >= 0.0 && sampleUv.y <= 1.0) { float zOrder = hash(gridPos + 12.34); if (zOrder > highestZ) { highestZ = zOrder; vec2 sampleUvScaled = sampleUv * uvScale; vec3 sampledTex = texture2D(tDiffuse, sampleUvScaled).rgb; paintedColor = vec4(sampledTex, alpha); } } } } } vec4 finalEffectColor = bgColor; if (paintedColor.a > 0.0) { finalEffectColor = vec4(mix(bgColor.rgb, paintedColor.rgb, paintedColor.a), max(bgColor.a, paintedColor.a)); } float blendFactor = clamp(Blend_With_Original / 100.0, 0.0, 1.0); gl_FragColor = mix(finalEffectColor, originalColor, blendFactor); }
