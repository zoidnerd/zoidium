precision highp float; precision highp int; uniform sampler2D tDiffuse; uniform vec2 uvScale; uniform vec2 resolution; varying vec2 vUv; varying vec2 vUvScaled; uniform float Render; uniform float Radius; uniform vec2 Center; uniform float Lock_Center_Tile; uniform float Rotate; uniform float Smearing; const float PI = 3.14159265358979323846; vec2 rot2(vec2 p, float a) { float s = sin(a), c = cos(a); return vec2(c*p.x - s*p.y, s*p.x + c*p.y); } vec2 rotateAround(vec2 p, vec2 pivot, float a) { return pivot + rot2(p - pivot, a); } float roundN(float x) { return (x < 0.0) ? ceil(x - 0.5) : floor(x + 0.5); } vec3 axialToCube(vec2 h) { float x = h.x; float z = h.y; float y = -x - z; return vec3(x, y, z); } vec2 cubeToAxial(vec3 c) { return vec2(c.x, c.z); } vec3 cubeRound(vec3 c) { float rx = roundN(c.x); float ry = roundN(c.y); float rz = roundN(c.z); float x_diff = abs(rx - c.x); float y_diff = abs(ry - c.y); float z_diff = abs(rz - c.z); if (x_diff > y_diff && x_diff > z_diff) rx = -ry - rz; else if (y_diff > z_diff) ry = -rx - rz; else rz = -rx - ry; return vec3(rx, ry, rz); } vec2 pixelToAxial(vec2 p, float size) { float q = (2.0/3.0) * (p.x / size); float r = (-1.0/3.0) * (p.x / size) + (0.5773502691896258) * (p.y / size); return vec2(q, r); } vec2 axialToPixel(vec2 h, float size) { float q = h.x; float r = h.y; float x = size * 1.5 * q; float y = size * 1.7320508075688772 * (r + 0.5*q); return vec2(x, y); } float sdHexFlat(vec2 p, float r) { p = abs(p); return max(p.y, dot(p, vec2(0.8660254037844386, 0.5))) - r; } vec2 foldAligned(vec2 p) { float r = length(p); float ang = atan(p.y, p.x); float wedge = PI / 3.0; float k = floor((ang + wedge*0.5) / wedge); float a = ang - k * wedge; a = abs(a); return vec2(cos(a), sin(a)) * r; } vec2 foldSeamless(vec2 p, float parity) { float r = length(p); float ang = atan(p.y, p.x); ang += parity * (PI / 6.0); float wedge = PI / 3.0; float k = floor((ang + wedge*0.5) / wedge); float a = ang - k * wedge; a = abs(a); return vec2(cos(a), sin(a)) * r; } vec2 perp(vec2 v) { return vec2(-v.y, v.x); } vec2 applySmear(vec2 warped, vec2 tileCenter, vec2 CenterPx, float size, float sm01) { vec2 d = tileCenter - CenterPx; float dist = length(d); vec2 dir = (dist > 1e-5) ? (d / dist) : vec2(1.0, 0.0); vec2 tanDir = perp(dir); float distN = dist / (size * 6.0 + 1e-5); float t = sm01 * clamp(distN, 0.0, 1.5); float par = dot(warped, dir); float per = dot(warped, tanDir); float drag = t * dist * 0.40; float stretch = 1.0 + t * (1.35 + 0.85 * clamp(distN, 0.0, 1.0)); float squeeze = 1.0 - t * 0.22; par = par * stretch - drag; per = per * squeeze; return dir * par + tanDir * per; } vec2 axialDir(int i) { if (i == 0) return vec2( 1.0, 0.0); if (i == 1) return vec2( 1.0, -1.0); if (i == 2) return vec2( 0.0, -1.0); if (i == 3) return vec2(-1.0, 0.0); if (i == 4) return vec2(-1.0, 1.0); return vec2( 0.0, 1.0); } int nearestNeighborDir(vec2 localGrid) { vec2 u = normalize(localGrid + vec2(1e-6, 0.0)); float best = -1e9; int bestI = 0; for (int i = 0; i < 6; i++) { vec2 dp = normalize(axialToPixel(axialDir(i), 1.0)); float s = dot(u, dp); if (s > best) { best = s; bestI = i; } } return bestI; } vec2 wrap01(vec2 uv) { return fract(uv); } vec4 sampleRepeat(vec2 samplePosPx) { vec2 uv = samplePosPx / resolution; uv = wrap01(uv); vec2 uvBuf = uv * uvScale; return texture2D(tDiffuse, uvBuf); } void main() { vec2 pos = vUv * resolution; float size = max(Radius, 0.0001); int mode = int(floor(Render + 0.5)); float lockC = step(0.5, Lock_Center_Tile); float theta = radians(Rotate); vec2 compCenter = resolution * 0.5; vec2 gridOrigin = mix(Center, compCenter, lockC); vec2 rotatePivot = mix(Center, compCenter, lockC); vec2 posGrid = rotateAround(pos, rotatePivot, -theta); vec2 gridOriginGrid = rotateAround(gridOrigin, rotatePivot, -theta); vec2 rel = posGrid - gridOriginGrid; vec2 axialF = pixelToAxial(rel, size); vec3 cube = axialToCube(axialF); vec3 cubeI = cubeRound(cube); vec2 cell = cubeToAxial(cubeI); vec2 tileCenterRel = axialToPixel(cell, size); vec2 tileCenterGrid = gridOriginGrid + tileCenterRel; vec2 localGrid = posGrid - tileCenterGrid; vec2 tileCenter = rotateAround(tileCenterGrid, rotatePivot, theta); float sm01 = clamp(Smearing / 100.0, 0.0, 1.0); vec2 warped; if (mode == 0) { warped = pos - tileCenter; } else { vec2 baseGrid = posGrid - tileCenterGrid; vec2 foldedGrid; if (mode == 1) { foldedGrid = foldAligned(baseGrid); } else { float parity = mod(cell.x + cell.y, 2.0); foldedGrid = foldSeamless(baseGrid, parity); } warped = rot2(foldedGrid, theta); } warped = applySmear(warped, tileCenter, Center, size, sm01); vec2 samplePos = Center + warped; vec4 col = sampleRepeat(samplePos); if (mode == 2) { float dEdge = sdHexFlat(localGrid, size); float seamW = max(1.5, size * 0.08); float w = smoothstep(0.0, seamW, -dEdge); int di = nearestNeighborDir(localGrid); vec2 nCell = cell + axialDir(di); vec2 nCenterRel = axialToPixel(nCell, size); vec2 nCenterGrid = gridOriginGrid + nCenterRel; vec2 nCenter = rotateAround(nCenterGrid, rotatePivot, theta); vec2 nBaseGrid = posGrid - nCenterGrid; float nParity = mod(nCell.x + nCell.y, 2.0); vec2 nFoldedGrid = foldSeamless(nBaseGrid, nParity); vec2 nWarped = rot2(nFoldedGrid, theta); nWarped = applySmear(nWarped, nCenter, Center, size, sm01); vec4 colN = sampleRepeat(Center + nWarped); col = mix(colN, col, w); gl_FragColor = col; return; } float alpha = 1.0; { float d = sdHexFlat(localGrid, size); float aa = 1.25; alpha = smoothstep(0.0, -aa, d); } gl_FragColor = vec4(col.rgb, col.a * alpha); }
