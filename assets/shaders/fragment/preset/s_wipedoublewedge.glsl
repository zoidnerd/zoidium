precision highp float; precision highp int; uniform sampler2D tDiffuse; varying vec2 vUvScaled; varying vec2 vUv; uniform vec2 resolution; uniform float time; uniform float Transition_Dir; uniform float Auto_Trans; uniform float Wipe_Percent; uniform float Wipe_Direction; uniform float Edge_Softness; uniform float Angle; uniform float Pointiness; uniform float Border_Width; uniform vec3 Border_Color; uniform float Border_Opacity; uniform float Border_Softness; uniform float Border_Shift; void main() { vec4 texColor = texture2D(tDiffuse, vUvScaled); vec4 bgColor = vec4(0.0); float progress; if (Auto_Trans == 1.0) { progress = clamp(time * 0.5, 0.0, 1.0); } else { progress = clamp(Wipe_Percent / 100.0, 0.0, 1.0); } float aspect = resolution.x / max(resolution.y, 1.0); vec2 p = vUv - 0.5; p.x *= aspect; float rad = -Angle * 3.14159265359 / 180.0; float c = cos(rad); float s = sin(rad); vec2 rp = vec2(p.x * c - p.y * s, p.x * s + p.y * c); float pt = max(Pointiness, 0.001); float F = abs(rp.x) - abs(rp.y) * pt; float maxF = -999.0; float minF = 999.0; vec2 tp, rtp; float tF; tp = vec2(0.5 * aspect, 0.5); rtp = vec2(tp.x * c - tp.y * s, tp.x * s + tp.y * c); tF = abs(rtp.x) - abs(rtp.y) * pt; maxF = max(maxF, tF); minF = min(minF, tF); tp = vec2(0.5 * aspect, -0.5); rtp = vec2(tp.x * c - tp.y * s, tp.x * s + tp.y * c); tF = abs(rtp.x) - abs(rtp.y) * pt; maxF = max(maxF, tF); minF = min(minF, tF); tp = vec2(-0.5 * aspect, 0.5); rtp = vec2(tp.x * c - tp.y * s, tp.x * s + tp.y * c); tF = abs(rtp.x) - abs(rtp.y) * pt; maxF = max(maxF, tF); minF = min(minF, tF); tp = vec2(-0.5 * aspect, -0.5); rtp = vec2(tp.x * c - tp.y * s, tp.x * s + tp.y * c); tF = abs(rtp.x) - abs(rtp.y) * pt; maxF = max(maxF, tF); minF = min(minF, tF); tp = vec2(0.5 * aspect, 0.0); rtp = vec2(tp.x * c - tp.y * s, tp.x * s + tp.y * c); tF = abs(rtp.x) - abs(rtp.y) * pt; maxF = max(maxF, tF); minF = min(minF, tF); tp = vec2(0.0, 0.5); rtp = vec2(tp.x * c - tp.y * s, tp.x * s + tp.y * c); tF = abs(rtp.x) - abs(rtp.y) * pt; maxF = max(maxF, tF); minF = min(minF, tF); float d; if (Wipe_Direction == 0.0) { if (progress < 0.5) d = mix(maxF, 0.0, progress * 2.0); else d = mix(0.0, minF, (progress - 0.5) * 2.0); } else { if (progress < 0.5) d = mix(minF, 0.0, progress * 2.0); else d = mix(0.0, maxF, (progress - 0.5) * 2.0); } float signed_dist = (Wipe_Direction == 0.0) ? (F - d) : (d - F); float grad_mag = sqrt(1.0 + (pt * pt)); float true_dist = signed_dist / grad_mag; float e_soft = max(Edge_Softness * 0.001, 0.001); float mixFactor = smoothstep(-e_soft, e_soft, true_dist); vec4 colOutgoing = (Transition_Dir == 0.0) ? texColor : bgColor; vec4 colIncoming = (Transition_Dir == 0.0) ? bgColor : texColor; vec4 baseColor = mix(colOutgoing, colIncoming, mixFactor); float border_dist = true_dist + (Border_Shift * 0.001); float bw = Border_Width * 0.001; float b_soft = max(Border_Softness * 0.001, 0.0001); float dist_from_center = abs(border_dist); float half_width = bw / 2.0; float border_alpha = 0.0; if (Border_Width > 0.0) { border_alpha = smoothstep(half_width + b_soft, half_width - b_soft, dist_from_center); border_alpha *= Border_Opacity; } vec4 finalColor; finalColor.rgb = mix(baseColor.rgb, Border_Color, border_alpha); finalColor.a = baseColor.a + border_alpha * (1.0 - baseColor.a); gl_FragColor = finalColor; }
