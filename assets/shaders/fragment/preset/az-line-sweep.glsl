precision highp float; precision highp int; uniform sampler2D tDiffuse; uniform vec2 resolution; varying vec2 vUv; varying vec2 vUvScaled; uniform float Completion; uniform float Direction; uniform float Thickness; uniform float Slant; uniform float Flip_Direction; vec2 aeDirVec(float deg) { float a = radians(deg); return normalize(vec2(sin(a), -cos(a))); } void main() { vec4 texel = texture2D(tDiffuse, vUvScaled); vec2 uvAE = vec2(vUv.x, 1.0 - vUv.y); vec2 pix = uvAE * resolution; vec2 center = 0.5 * resolution; vec2 p = pix - center; float comp = clamp(Completion / 100.0, 0.0, 1.0); if (Flip_Direction >= 0.5) comp = 1.0 - comp; float thick = max(1.0, Thickness); vec2 m = aeDirVec(Direction); vec2 t = vec2(-m.y, m.x); vec2 c0 = vec2(-center.x, -center.y); vec2 c1 = vec2( center.x, -center.y); vec2 c2 = vec2(-center.x, center.y); vec2 c3 = vec2( center.x, center.y); float vm0 = dot(c0, m), vm1 = dot(c1, m), vm2 = dot(c2, m), vm3 = dot(c3, m); float vt0 = dot(c0, t), vt1 = dot(c1, t), vt2 = dot(c2, t), vt3 = dot(c3, t); float vMinBase = min(min(vm0, vm1), min(vm2, vm3)); float vMaxBase = max(max(vm0, vm1), max(vm2, vm3)); float uMinBase = min(min(vt0, vt1), min(vt2, vt3)); float uMaxBase = max(max(vt0, vt1), max(vt2, vt3)); float vSpanBase = max(1.0, (vMaxBase - vMinBase)); float uSpanBase = max(1.0, (uMaxBase - uMinBase)); float stepSize = thick; float u = dot(p, t); float uQ = floor(u / stepSize + 0.5) * stepSize; float slope = (Slant / 50.0) * (vSpanBase / uSpanBase); slope = clamp(slope, 0.0, 6.0); float proj = dot(p, m) + slope * uQ; float p0 = dot(c0, m) + slope * (floor(dot(c0, t)/stepSize + 0.5) * stepSize); float p1 = dot(c1, m) + slope * (floor(dot(c1, t)/stepSize + 0.5) * stepSize); float p2 = dot(c2, m) + slope * (floor(dot(c2, t)/stepSize + 0.5) * stepSize); float p3 = dot(c3, m) + slope * (floor(dot(c3, t)/stepSize + 0.5) * stepSize); float minP = min(min(p0, p1), min(p2, p3)); float maxP = max(max(p0, p1), max(p2, p3)); float normPos = (proj - minP) / max(1.0, (maxP - minP)); float alpha = step(comp, normPos); texel.rgb *= alpha; texel.a *= alpha; gl_FragColor = texel; }
