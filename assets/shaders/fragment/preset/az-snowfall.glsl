precision highp float; precision highp int; uniform sampler2D tDiffuse; varying vec2 vUvScaled; uniform vec2 resolution; uniform float Flakes; uniform float Size; uniform float Size_Variation; uniform float Scene_Depth; uniform float Speed; uniform float Speed_Variation; uniform float Wind; uniform float Wind_Variation; uniform float Spread; uniform float Wiggle_Amount; uniform float Wiggle_Variation; uniform float Wiggle_Frequency; uniform float Frequency_Variation; uniform float Stochastic_Wiggle; uniform float Flake_Flatness; uniform vec3 Color; uniform float Opacity; uniform float Time; float rand(float n) { return fract(sin(n) * 43758.5453123); } void main() { vec4 texel = texture2D(tDiffuse, vUvScaled); vec2 uv = vUvScaled; float snowAlpha = 0.0; for (int i = 0; i < 300; i++) { if (float(i) >= Flakes) break; float seed = float(i); float x = rand(seed * 12.9898); float y = rand(seed * 78.233); float z = rand(seed * 45.164) * Scene_Depth; float depthFactor = 1.0 / (1.0 + z * 0.01); float sizeVariation = 1.0 + (rand(seed * 55.5) - 0.5) * (Size_Variation / 100.0); float flakeSize = (Size / resolution.x) * depthFactor * sizeVariation; float flakeLength = flakeSize * (Flake_Flatness / 100.0); float speedVariation = 1.0 + (rand(seed * 66.6) - 0.5) * (Speed_Variation / 100.0); float flakeSpeed = Speed * depthFactor * speedVariation; float depthOpacity = mix(1.0, 0.3, z / Scene_Depth); float baseAngle = radians(Wind); float angleOffset = radians((rand(seed * 99.9) - 0.5) * 2.0 * Wind_Variation); float finalAngle = baseAngle + angleOffset; vec2 fallDir = vec2(sin(finalAngle), -cos(finalAngle)); x += fallDir.x * flakeSpeed * Time / resolution.x; y += fallDir.y * flakeSpeed * Time / resolution.y; float spreadAngle = radians((rand(seed * 77.7) - 0.5) * 360.0); vec2 spreadDir = vec2(cos(spreadAngle), sin(spreadAngle)); x += spreadDir.x * Spread * Time / resolution.x; y += spreadDir.y * Spread * Time / resolution.y; float wiggleAmp = Wiggle_Amount * (1.0 + (rand(seed * 88.8) - 0.5) * (Wiggle_Variation / 100.0)); float freqVariation = 1.0 + (rand(seed * 101.1) - 0.5) * (Frequency_Variation / 100.0); float wigglePhase = Time * Wiggle_Frequency * freqVariation + seed; x += sin(wigglePhase) * wiggleAmp / resolution.x; if (Stochastic_Wiggle > 0.5) { y += (rand(seed * 222.2) - 0.5) * wiggleAmp * 0.2 / resolution.y; } x = mod(x, 1.0); y = mod(y, 1.0); mat2 rotation = mat2(cos(finalAngle), -sin(finalAngle), sin(finalAngle), cos(finalAngle)); vec2 diff = rotation * (uv - vec2(x, y)); float blurFactor = mix(0.0, 0.004, z / Scene_Depth); float flake = smoothstep(flakeSize + blurFactor, 0.0, abs(diff.x)) * smoothstep(flakeLength + blurFactor, 0.0, abs(diff.y)); snowAlpha += flake * depthOpacity; } snowAlpha = clamp(snowAlpha * Opacity, 0.0, 1.0); vec4 snowColor = vec4(Color, snowAlpha); gl_FragColor = mix(texel, snowColor, snowAlpha); }
