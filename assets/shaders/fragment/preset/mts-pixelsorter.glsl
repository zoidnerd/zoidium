precision highp float; precision highp int; uniform sampler2D tDiffuse; varying vec2 vUvScaled; uniform vec2 resolution; uniform float Mode; uniform float Direction; uniform float Threshold; uniform float Conditional_Sort; const int MAX_SEARCH = 50; float getValue(vec3 color, float mode) { if (mode < 0.5) { return dot(color, vec3(0.299, 0.587, 0.114)); } else if (mode < 1.5) { float cMax = max(max(color.r, color.g), color.b); float cMin = min(min(color.r, color.g), color.b); return (cMax > 0.0) ? (cMax - cMin) / cMax : 0.0; } else if (mode < 2.5) { float cMax = max(max(color.r, color.g), color.b); float cMin = min(min(color.r, color.g), color.b); float delta = cMax - cMin; if (delta == 0.0) return 0.0; float h = 0.0; if (cMax == color.r) h = mod((color.g - color.b) / delta, 6.0); else if (cMax == color.g) h = (color.b - color.r) / delta + 2.0; else if (cMax == color.b) h = (color.r - color.g) / delta + 4.0; return h / 6.0; } else { return (color.r + color.g + color.b) / 3.0; } } vec2 getDir(float dir) { if (dir < 0.5) return vec2(1.0, 0.0); else if (dir < 1.5) return vec2(-1.0, 0.0); else if (dir < 2.5) return vec2(0.0, -1.0); else return vec2(0.0, 1.0); } void main() { vec2 dirVec = getDir(Direction) / resolution; float thresh = Threshold / 255.0; vec4 myCol = texture2D(tDiffuse, vUvScaled); float myVal = getValue(myCol.rgb, Mode); if (myVal < thresh) { gl_FragColor = myCol; return; } int startIdx = 0; int endIdx = 0; for (int i = 1; i <= MAX_SEARCH; i++) { vec2 uv = vUvScaled - dirVec * float(i); if(uv.x < 0.0 || uv.x > 1.0 || uv.y < 0.0 || uv.y > 1.0) break; if(getValue(texture2D(tDiffuse, uv).rgb, Mode) < thresh) break; startIdx = i; } for (int i = 1; i <= MAX_SEARCH; i++) { vec2 uv = vUvScaled + dirVec * float(i); if(uv.x < 0.0 || uv.x > 1.0 || uv.y < 0.0 || uv.y > 1.0) break; if(getValue(texture2D(tDiffuse, uv).rgb, Mode) < thresh) break; endIdx = i; } vec4 resultCol = myCol; if (Conditional_Sort > 0.5) { for (int i = -MAX_SEARCH; i <= MAX_SEARCH; i++) { if (i < -startIdx || i > endIdx) continue; vec2 pUv = vUvScaled + dirVec * float(i); vec4 pCol = texture2D(tDiffuse, pUv); float pVal = getValue(pCol.rgb, Mode); int pRank = 0; for (int j = -MAX_SEARCH; j <= MAX_SEARCH; j++) { if (j < -startIdx || j > endIdx) continue; vec2 oUv = vUvScaled + dirVec * float(j); float oVal = getValue(texture2D(tDiffuse, oUv).rgb, Mode); if (oVal > pVal) pRank++; else if (oVal == pVal && j < i) pRank++; } if (pRank == startIdx) { resultCol = pCol; break; } } } else { int myRank = 0; for (int j = -MAX_SEARCH; j <= MAX_SEARCH; j++) { if (j < -startIdx || j > endIdx) continue; vec2 oUv = vUvScaled + dirVec * float(j); float oVal = getValue(texture2D(tDiffuse, oUv).rgb, Mode); if (oVal > myVal) myRank++; else if (oVal == myVal && j < 0) myRank++; } float offset = float(myRank - startIdx); vec2 displaceUv = clamp(vUvScaled + dirVec * offset, 0.0, 1.0); resultCol = texture2D(tDiffuse, displaceUv); } gl_FragColor = resultCol; }
