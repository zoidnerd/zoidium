precision highp float; precision highp int; uniform sampler2D tDiffuse; uniform vec2 uvScale; uniform vec2 resolution; varying vec2 vUv; varying vec2 vUvScaled; uniform float Samples; uniform float Strength; uniform float Separation; uniform float Edge_Attraction_Power; uniform float Direction; uniform float Edge_Mode; const int MAX_SAMPLES = 64; const float EPS = 1e-5; float mirrorCoord(float x) { float m = mod(x, 2.0); if (m < 0.0) m += 2.0; return 1.0 - abs(m - 1.0); } vec2 applyEdgeMode(vec2 uv, out float valid) { valid = 1.0; if (Edge_Mode < 0.5) { if (uv.x < 0.0 || uv.x > 1.0 || uv.y < 0.0 || uv.y > 1.0) { valid = 0.0; } return clamp(uv, 0.0, 1.0); } else if (Edge_Mode < 1.5) { return vec2(mirrorCoord(uv.x), mirrorCoord(uv.y)); } else if (Edge_Mode < 2.5) { return clamp(uv, 0.0, 1.0); } else { return fract(uv); } } vec4 sampleSource(vec2 uv) { float valid; vec2 eUv = applyEdgeMode(uv, valid); vec4 c = texture2D(tDiffuse, eUv * uvScale); if (Edge_Mode < 0.5) { c *= valid; } return c; } float edgeCurveFromRadius(float radius01) { return pow(radius01, max(Edge_Attraction_Power, 0.001)); } void getVectors( vec2 uv, out vec2 blurVec, out vec2 redVec, out vec2 blueVec ) { vec2 center = vec2(0.5, 0.5); vec2 deltaPx = (uv - center) * resolution; float distPx = length(deltaPx); vec2 dirPx = (distPx > EPS) ? (deltaPx / distPx) : vec2(1.0, 0.0); float maxDistPx = length(0.5 * resolution); float radius01 = (maxDistPx > EPS) ? clamp(distPx / maxDistPx, 0.0, 1.0) : 0.0; float edgeCurve = edgeCurveFromRadius(radius01); float basePx = Strength * edgeCurve; float sepPx = (Separation / 100.0) * Strength * edgeCurve; vec2 baseUv = (dirPx * basePx) / resolution; vec2 sepUv = (dirPx * sepPx) / resolution; float mode = floor(Direction + 0.5); if (mode < 0.5) { blurVec = baseUv; redVec = sepUv * 0.5; blueVec = -sepUv * 0.5; } else if (mode < 1.5) { blurVec = -baseUv; redVec = -sepUv * 0.5; blueVec = sepUv * 0.5; } else { blurVec = baseUv; redVec = sepUv * 0.5; blueVec = -sepUv * 0.5; } } void main() { vec2 uv = vUv; vec4 base = sampleSource(uv); float sCount = clamp(floor(Samples + 0.5), 1.0, float(MAX_SAMPLES)); if (Strength <= 0.0 && Separation <= 0.0) { gl_FragColor = base; return; } vec2 blurVec; vec2 redVec; vec2 blueVec; getVectors(uv, blurVec, redVec, blueVec); float sumR = 0.0; float sumG = 0.0; float sumB = 0.0; float sumA = 0.0; float sumW = 0.0; float mode = floor(Direction + 0.5); for (int i = 0; i < MAX_SAMPLES; i++) { if (float(i) >= sCount) break; float t = 0.0; if (sCount > 1.0) { t = float(i) / (sCount - 1.0); } float shift = 0.0; float w = 1.0; if (mode < 0.5) { shift = t; } else if (mode < 1.5) { shift = t; } else { shift = (t * 2.0) - 1.0; w = 1.0 - abs(shift); w = max(w, 0.001); } vec2 samplePos = uv + blurVec * shift; vec2 rPos = samplePos + redVec; vec2 gPos = samplePos; vec2 bPos = samplePos + blueVec; vec4 cR = sampleSource(rPos); vec4 cG = sampleSource(gPos); vec4 cB = sampleSource(bPos); sumR += cR.r * w; sumG += cG.g * w; sumB += cB.b * w; sumA += max(max(cR.a, cG.a), cB.a) * w; sumW += w; } vec4 outCol = vec4(sumR, sumG, sumB, sumA) / sumW; if (Edge_Mode >= 0.5) { outCol.a = max(outCol.a, base.a); } gl_FragColor = outCol; }
