precision highp float; precision highp int; uniform sampler2D tDiffuse; varying vec2 vUvScaled; uniform vec2 resolution; uniform float Radius_percent; uniform float Position_X; uniform float Position_Y; uniform float Position_Z; uniform vec3 Rotation_degs; uniform float Rotation_Order_f; uniform float Render_Mode_f; const float PI = 3.141592653589793; float d2r(float d){ return d * PI / 180.0; } mat3 rotX(float a){ float c=cos(a), s=sin(a); return mat3( 1.0,0.0,0.0, 0.0,c,-s, 0.0,s,c ); } mat3 rotY(float a){ float c=cos(a), s=sin(a); return mat3( c,0.0,s, 0.0,1.0,0.0, -s,0.0,c ); } mat3 rotZ(float a){ float c=cos(a), s=sin(a); return mat3( c,-s,0.0, s, c,0.0, 0.0,0.0,1.0 ); } mat3 composeRotation(int o, vec3 a){ mat3 RX=rotX(a.x), RY=rotY(a.y), RZ=rotZ(a.z); if(o==0) return RZ*RY*RX; if(o==1) return RY*RZ*RX; if(o==2) return RZ*RX*RY; if(o==3) return RX*RZ*RY; if(o==4) return RY*RX*RZ; return RX*RY*RZ; } mat3 transpose3(mat3 m){ return mat3( m[0][0], m[1][0], m[2][0], m[0][1], m[1][1], m[2][1], m[0][2], m[1][2], m[2][2] ); } vec4 over(vec4 A, vec4 B){ return A + (1.0 - A.a) * B; } vec4 sampleCylinder(vec3 ro, vec3 rd, float t, bool backFace){ vec3 ph = ro + rd * t; float u = atan(ph.x, ph.z) / (2.0 * PI) + 0.5; u = fract(u + 0.5); if(backFace){ u = fract(1.0 - u); } float v = 0.5 - 0.5 * ph.y; if(v < 0.0 || v > 1.0) return vec4(0.0); return texture2D(tDiffuse, vec2(u, v)); } void main(){ int rotOrder = int(floor(Rotation_Order_f + 0.5)); int renderMode = int(floor(Render_Mode_f + 0.5)); float aspect = resolution.x / resolution.y; float camDist = 2.0; vec2 uv = vUvScaled; vec2 screen = (uv - 0.5) * vec2(aspect * 2.0, 2.0); vec3 rayOrigin = vec3(0.0, 0.0, camDist); vec3 target = vec3(screen.x, -screen.y, 0.0); vec3 rayDir = normalize(target - rayOrigin); vec3 cylCenter = vec3( ((Position_X / resolution.x) - 0.5) * (aspect * 2.0), -((Position_Y / resolution.y) - 0.5) * 2.0, (Position_Z / resolution.y) * 2.0 ); float R = max(0.001, (Radius_percent * 0.01) * 1.0); vec3 ang = vec3(d2r(Rotation_degs.x), d2r(Rotation_degs.y), d2r(Rotation_degs.z)); mat3 Rm = composeRotation(rotOrder, ang); mat3 Ri = transpose3(Rm); vec3 ro = Ri * (rayOrigin - cylCenter); vec3 rd = Ri * rayDir; vec2 roxz = ro.xz; vec2 rdxz = rd.xz; float a = dot(rdxz, rdxz); float b = 2.0 * dot(roxz, rdxz); float c = dot(roxz, roxz) - R * R; float d = b*b - 4.0*a*c; if(d < 0.0 || a < 1e-8){ gl_FragColor = vec4(0.0); return; } float sqrtD = sqrt(d); float tNear = (-b - sqrtD) / (2.0*a); float tFar = (-b + sqrtD) / (2.0*a); if(tFar < 0.0){ gl_FragColor = vec4(0.0); return; } if(tNear > tFar){ float tmp = tNear; tNear = tFar; tFar = tmp; } bool hasNear = (tNear > 0.0); bool hasFar = (tFar > 0.0); if(renderMode == 1){ if(!hasNear){ gl_FragColor = vec4(0.0); return; } gl_FragColor = sampleCylinder(ro, rd, tNear, false); return; } if(renderMode == 2){ if(!hasFar){ gl_FragColor = vec4(0.0); return; } gl_FragColor = sampleCylinder(ro, rd, tFar, true); return; } vec4 colFront = hasNear ? sampleCylinder(ro, rd, tNear, false) : vec4(0.0); vec4 colBack = hasFar ? sampleCylinder(ro, rd, tFar, true) : vec4(0.0); gl_FragColor = over(colFront, colBack); }
